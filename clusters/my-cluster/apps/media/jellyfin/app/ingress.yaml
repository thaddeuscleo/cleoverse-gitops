apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: jellyfin-ingress
  namespace: media
  annotations:
    hajimari.io/icon: arcticons:jellyfin
    hajimari.io/enable: true
    hajimari.io/appName: "Jellyfin"
    hajimari.io/group: "Media"
spec:
  ingressClassName: traefik
  rules:
  - host: jellyfin.cleoverse.home
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: jellyfin-svc
            port:
              number: 80